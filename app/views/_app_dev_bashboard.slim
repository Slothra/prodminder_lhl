.dev-mode
  .container

    a.count-switches href="#" Count On Switches with screening_id

    p Session.inspect
    pre
      = session.inspect

    p
      | @current_user_logged_in
      code #{ @current_user_logged_in }

    br
    pre
      = @screenings = Screening.all.inspect
      = @conditions = Condition.all.inspect
